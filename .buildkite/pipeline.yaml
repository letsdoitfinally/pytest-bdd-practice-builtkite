steps:
  - label: ":docker: Build and Run Tests"
    commands:
      - pwd
      - ls -la
      - docker build -t pytest-bdd:latest .
      - docker images
      - docker run --rm pytest-bdd:latest

    agents:
      queue: "linux-small"